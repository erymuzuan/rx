@model Bespoke.SphCommercialSpaces.Domain.ComboBox
<div class="control-group">
    <label for="@Model.ElementId" class="control-label">@Model.Label</label>
    <div class="controls">
        <select data-bind="@(Html.Raw(Model.GetKnockoutBindingExpression()))" id="@Model.ElementId" name="@(Model.CssClass + " "+ Model.Size)">
            @foreach (var op in Model.ComboBoxItemCollection)
            {
                <option value="@op.Value">@op.Caption</option>
            }
        </select>
    </div>
</div>
