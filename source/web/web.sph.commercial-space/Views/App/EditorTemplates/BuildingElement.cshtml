@model Bespoke.SphCommercialSpaces.Domain.BuildingElement

<div class="control-group">
   <label class="control-label">@Model.Label</label>
    <div class="controls">
        <select data-bind="options: $root.buildingOptions, optionsValue: 'Item1', optionsText: 'Item2', optionsCaption: '[Sila pilih]', value: $root.selectedBuilding"></select>
        <span class="help-inline">@Model.HelpText</span>
    </div>
</div>
