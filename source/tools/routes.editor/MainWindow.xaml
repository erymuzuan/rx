<Window x:Class="routes.editor.MainWindow"
          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
        Icon="65.ico"
        WindowState="Maximized"
        xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation" xmlns:vm="clr-namespace:routes.editor"
        Title="{Binding FileName}" Height="350" Width="525">
    <Grid d:DataContext="{d:DesignInstance vm:MainViewModel}" >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <telerik:RadButton Margin="5" Content="Open" Command="{Binding OpenCommand}" />
        <telerik:RadGridView  Margin="5" Grid.Row="1" ItemsSource="{Binding RouteCollection}">
            
        </telerik:RadGridView>
        <telerik:RadButton Margin="5" Grid.Row="2" Content="Save" Command="{Binding SaveCommand}" />
    </Grid>
</Window>
