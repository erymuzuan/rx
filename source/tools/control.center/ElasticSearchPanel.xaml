<UserControl x:Class="Bespoke.Sph.ControlCenter.ElasticSearchPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="Elastic Search" Style="{StaticResource Heading2}" />
        <StackPanel  Orientation="Horizontal" Grid.Row="1">
            <Button IsEnabled="False" Width="60" Height="30" HorizontalAlignment="Left" Margin="0">
                <Image Source="play.png" Stretch="None"/>
            </Button>
            <Button Width="60" Height="30" HorizontalAlignment="Left" Margin="0">
                <Image Source="stop.png" Stretch="None"/>
            </Button>
        </StackPanel>
        <Button Grid.Row="1" HorizontalAlignment="Right" Content="Settings" Style="{StaticResource Hyperlink}"></Button>
        <TextBox Grid.Row="2" IsReadOnly="True" AcceptsReturn="True" Text="Waiting to start..." />
    </Grid>
</UserControl>
