<UserControl x:Class="Bespoke.Sph.ControlCenter.UpdaterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             xmlns:vm="clr-namespace:Bespoke.Sph.ControlCenter.ViewModel"
             >
    <StackPanel d:DataContext="{d:DesignData vm:UpdaterViewModel}" Orientation="Horizontal">
        <Button IsEnabled="{Binding IsBusy, Converter={StaticResource BooleanInvertConverter}}" Content="Load update package" Command="{Binding LoadUpdateCommand}"/>
        <Button IsEnabled="{Binding IsBusy, Converter={StaticResource BooleanInvertConverter}}" Content="Check for updates" Command="{Binding CheckUpdateCommand}"/>
    </StackPanel>
</UserControl>
