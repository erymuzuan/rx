<UserControl x:Class="Bespoke.Sph.ControlCenter.ProjectSettingsUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignWidth="700" Background="White">
    <Grid>
        <StackPanel>
            <GroupBox Header="SPH Application">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="20*"/>
                        <ColumnDefinition Width="80*"/>
                    </Grid.ColumnDefinitions>
                    <Label Grid.Row="0" Grid.Column="0" Content="Application Name"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding ApplicationName}"/>
                    <Label Grid.Row="1" Grid.Column="0" Content="Project Directory"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding ProjectDirectory}"/>
                    <Label Grid.Row="2" Grid.Column="0" Content="IIS Port"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Port,Mode=TwoWay}"/>
                </Grid>
            </GroupBox>
            <GroupBox Header="IIS Express">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="20*"/>
                        <ColumnDefinition Width="80*"/>
                    </Grid.ColumnDefinitions>
                    <Label Grid.Row="0" Grid.Column="0" Content="IIS Express Directory"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding IisExpressDirectory}"/>
                </Grid>
            </GroupBox>
            <GroupBox Header="SQL LocalDB">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="20*"/>
                        <ColumnDefinition Width="80*"/>
                    </Grid.ColumnDefinitions>
                    <Label Grid.Row="0" Grid.Column="0" Content="LocalDB Instance Name"/>
                    <ComboBox IsEditable="True" Name="sqlIntancesCombobox" Grid.Row="0" Grid.Column="1" Text="{Binding SqlLocalDbName}"/>
                </Grid>
            </GroupBox>
            <GroupBox Header="RabbitMQ">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="20*"/>
                        <ColumnDefinition Width="80*"/>
                    </Grid.ColumnDefinitions>
                    <Label Grid.Row="0" Grid.Column="0" Content="RabbitMQ Directory"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding RabbitMqDirectory}"/>
                    <Label Grid.Row="1" Grid.Column="0" Content="Username"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding RabbitmqUserName}"/>
                    <Label Grid.Row="2" Grid.Column="0" Content="Password"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding RabbitmqPassword}"/>
                </Grid>
            </GroupBox>
            <GroupBox Header="Elastic Search">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="20*"/>
                        <ColumnDefinition Width="80*"/>
                    </Grid.ColumnDefinitions>
                    <Label Grid.Row="0" Grid.Column="0" Content="JAVA Home"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding JavaHome}"/>
                    <Label Grid.Row="1" Grid.Column="0" Content="ElasticSearch Home"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding ElasticSearchHome}"/>
                </Grid>
            </GroupBox>
            <Button HorizontalAlignment="Right" Content="Save Settings" 
                            Command="{Binding SaveSettingsCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
